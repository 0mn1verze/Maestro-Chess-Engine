#include <cstring>
#include <iostream>

#include "perft.hpp"
#include "uci.hpp"

using namespace Maestro;

int main() {

  UCI uci;

  uci.loop();

  // Position pos;
  // BoardState st{};
  // const std::string fen(trickyPos);
  // pos.set(fen, st);

  // perftTest(pos, 6);

  return 0;
}